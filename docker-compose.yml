services:
  pal:
    build: .
    ports:
      - "8090:8090"
    volumes:
      - ./files:/app/files
      - ./prompts:/app/prompts
    environment:
      - PAL_SERVER_PORT=8090
      - PAL_LOG_LEVEL=INFO
